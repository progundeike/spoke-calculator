version: 2.1

#オーブを使用してシンプルな構成にする
orbs:
  heroku: circleci/heroku@1.2.6

workflows:
  heroku_deploy:
    jobs:
      - build
      - deploy
      - heroku/deploy-via-git:
          requires:
            - build
          filters:
            branches:
              only: main
